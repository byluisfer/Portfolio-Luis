<!doctype html>
<html lang="de">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Luis Fernández</title>
	</head>
	<body class="bg-black noise-bg">
		<!-- Canvas to the background -->
		<canvas id="backgroundCanvas" class="absolute top-0 left-0 w-full h-full -z-10"></canvas>

		<!-- Cursor -->
		<div class="cursor-point"></div>
	
		<slot />

		<!-- Script to generate the background -->
		<script src="/scripts/background.js" defer></script>

		<!-- Script to generate the cursor -->
		<script src="/scripts/cursor.js" defer></script>
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}

	.cursor-point {
		position: fixed;
		width: 14px;
		height: 14px;
		background-color: white;
		border-radius: 50%;
		transform: translate(-50%, -50%);
		z-index: 9999;
		pointer-events: none;
	}

	@media (max-width: 1024px) {
		.cursor-point {
			display: none;
		}
	}

	.noise-bg {
    position: relative;
	}

	.noise-bg::after {
		content: "";
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: url("/assets/noise.png");
		opacity: 0.05;
		pointer-events: none;
		z-index: -1;
	}

	@font-face {
		font-family: "InstrumentSerif";
		src: url("/fonts/Instrument-Serif.woff2") format("woff2");
		font-weight: normal;
		font-style: normal;
	}

	@font-face {
		font-family: "MonaSans";
		src: url("/fonts/Mona-Sans-SemiBold.woff2") format("woff2");
		font-weight: bold;
		font-style: normal;
	}
</style>
