<section class="mt-40">
  <div class="relative text-white flex flex-col items-center justify-center">
    <!-- Sección del título -->
    <h2 class="text-lg font-InterTight tracking-widest text-white drop-shadow-[0_0_6px_rgba(255,255,255,0.8)] items-center">
      <span class="mr-3">✦</span>
      KONTAKT
      <span class="ml-3">✦</span>
    </h2>

    <!-- Texto grande con gradiente -->
    <h3 class="text-5xl font-NyghtSerif mt-4 text-transparent bg-clip-text px-2 bg-gradient-to-b from-white to-accent drop-shadow-[0_0_6px_rgba(255,255,255,0.3)]">
      Verbinden wir uns!
    </h3>

    <!-- Contenedor con fondo oscuro y ruido -->
    <div class="relative mt-10 w-full max-w-5xl px-8 py-16 bg-nose rounded-3xl shadow-lg text-center border-2 border-navbarBorder overflow-hidden">
      
      <!-- Efecto de luz en el fondo -->
      <div class="absolute inset-0 bg-[radial-gradient(circle,rgba(255,255,255,0.1)_0%,rgba(0,0,0,0)60%)] pointer-events-none"></div>

      <!-- Contenido -->
      <h1 class="text-4xl sm:text-6xl font-bold text-white leading-tight relative">
        Kontaktieren Sie mich
      </h1>

      <p class="text-gray-400 text-lg mt-6 max-w-2xl mx-auto relative">
        Haben Sie eine Frage, eine Idee oder ein Projekt, bei dem Sie Hilfe benötigen? Kontaktieren Sie mich!
      </p>

      <button onclick="window.location.href='mailto:contact@luisfernandez.info?subject=Consulta&body=Hola,%20quisiera%20hablar%20contigo%20sobre...'"
        class="relative z-10 px-20 py-4 mt-10 text-xl rounded-full text-black bg-white font-semibold cursor-pointer shadow-lg border border-navbarBorder transition duration-300
        hover:shadow-[0_0_20px_rgba(255,255,255,0.8)] hover:border-white">
        SAG HALLO
      </button>
    </div>
  </div>
</section>